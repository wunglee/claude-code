# Project Memory

This is a test memory file for the claude-code project.

## Project Structure

- This is a CLI tool for Claude Code
- Uses React + Ink for terminal UI
- Built with Bun and bundled with esbuild

## Development Notes

- Always use `bun` for running scripts
- The bundled output is in `dist/cli.mjs`
- SDK headers patch is applied via postinstall hook
